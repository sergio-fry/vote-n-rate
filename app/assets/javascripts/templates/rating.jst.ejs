<center class="rendered">
  <h1 class="rating-title"><%= title %></h1>

  <div class="description"><%= description %></div>
  <br />
  <% if(can_edit) { %>
    <div class="controls btn btn-edit btn-primary" style="opacity: 0.0">Редактировать</div>
  <% } %>
</center>
